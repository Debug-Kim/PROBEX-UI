// Web3 hooks (stub). The only layer that imports wagmi — components consume these
// instead, so wagmi stays out of the component tree.
// TODO: implement wallet-connection, market/vault contract, balance, and tx-status hooks.

export {} // module marker until exports exist
