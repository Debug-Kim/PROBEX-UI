// Web3 providers (stub). Will host the Wagmi v2 + viem provider for Polygon
// (mainnet 137 / Amoy 80002) wrapping QueryProvider.
// TODO: add WagmiProvider once NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID is configured.

export {} // module marker until exports exist
