// Consensus transformers (stub). Adapters translate API → app schema;
// transformers enrich that schema with derived fields (e.g. staleness, bias
// alignment) and normalize history for charts.
// TODO: implement enrich/normalize-history/diff helpers.

export {} // module marker until exports exist
