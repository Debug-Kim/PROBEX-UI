// Consensus adapters (stub). Translate raw Consensus Engine responses into typed
// ConsensusState — isolating API schema changes from UI, validating enums, and
// normalizing 0–100 percentages to 0–1 floats.
// TODO: implement REST, WebSocket, and mock adapters behind IConsensusAdapter.

export {} // module marker until exports exist
